DATA SEGMENT
	NUM1 DB 11H;
	NUM2 DB 00H;
DATA ENDS

CODE SEGMENT
        ASSUME CS:CODE, DS:DATA
START:
	MOV CX, DATA;
	MOV DS, CX;
	

        MOV BX, NUM1;
	MOV NUM2, BX;

	INT 21;
 CODE ENDS
END START

