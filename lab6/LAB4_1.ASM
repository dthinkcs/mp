DATA SEGMENT
	PRODUCT DW ?
        A DB 05H
        B DB 9H
DATA ENDS

CODE SEGMENT
ASSUME CS:CODE, DS:DATA
START:
	MOV DX, DATA
	MOV DS, DX
	
        MOV AL, A
	MUL B
	MOV PRODUCT, AX
	
	MOV AH, 4CH
	INT 21H

CODE ENDS
END START
