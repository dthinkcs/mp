DATA SEGMENT
	NUM1 DB ?; // NUM1 TO BE COPIED INTO
	
DATA ENDS

CODE SEGMENT
        ASSUME CS:CODE, DS:DATA
START:
	MOV CX, DATA; // DS IS SET TO DATA
	MOV DS, CX;
	

        MOV BX, 12H; // FILL UP BX REGISTER
	MOV NUM1, BX; // COPY FROM REGISTER TO NUM1

	INT 21;
 CODE ENDS
END START

