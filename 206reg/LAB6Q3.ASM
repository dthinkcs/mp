DATA SEGMENT
NUM DW 25,15
LCM DW 2 DUP (?)
DATA ENDS
CODE SEGMENT
ASSUME CS:CODE,DS:DATA
START:
MOV AX,DATA
MOV DS,AX
MOV DX,0
MOV AX,NUM
MOV BX,NUM+2
BACK:PUSH AX
PUSH DX
DIV BX
CMP DX,0
JZ DOWN
POP DX
POP AX
ADD AX,NUM
ADC DX,0
JMP BACK
DOWN:POP LCM+2
POP LCM
MOV AH,4CH
INT 21H
CODE ENDS
END START
