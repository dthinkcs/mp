 DATA SEGMENT
 NUM1 DW 0090H
 NUM2 DW 0040H
 NUM3 DW 0050H
 NUM4 DW 0060H
 GCD DW ?
 DATA ENDS
 CODE SEGMENT
 ASSUME CS:CODE,DS:DATA
 START: MOV AX,DATA
 MOV DS,AX
 MOV AX,NUM1
 MOV DX,NUM2
 CALL GCD_PROC
 MOV CX,BX
 MOV AX,NUM3
 MOV BX,NUM4
 CALL GCD_PROC
 MOV AX,CX
 CALL GCD_PROC
 MOV GCD,BX
 MOV AH,4CH
 INT 21H
 GCD_PROC PROC
 GCD1:
 CMP AX,BX
 JC BIGBX
 DIVAXBX:
 MOV DX,0
 DIV BX
 CMP DX,0
 JE EXIT
 MOV AX,DX
 JMP GCD1
 BIGBX:
 XCHG AX,BX
 JMP DIVAXBX
 EXIT: RET
 GCD_PROC ENDP
 CODE ENDS
 END START
 
