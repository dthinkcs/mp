STACK SEGMENT
DW 100 DUP (?)
TOS LABEL WORD
STACK ENDS
DATA SEGMENT
NUM DB 0AH
RES DW ?
DATA ENDS
CODE SEGMENT
ASSUME CS:CODE,DS:DATA,SS:STACK
START: MOV AX,DATA
MOV DS,AX
MOV AX,STACK
MOV SS,AX
LEA SI,TOS
MOV AL,NUM
MOV AH,0
CALL FACT
MOV AH,4CH
INT 21H
FACT PROC
MOV BX,0001H
CMP AX,BX
JE EXIT
PUSH AX
DEC AX
CALL FACT
POP AX
MUL RES
MOV RES,AX
RET
EXIT: MOV RES,01H
RET
FACT ENDP
CODE ENDS
END START
