DATA SEGMENT
        HEX DB ?
        BCD DW ?
DATA ENDS

CODE SEGMENT
ASSUME CS:CODE, DS:DATA
START:
	MOV DX, DATA
	MOV DS, DX
	
	MOV SI, 0
	MOV AL, HEX
	MOV AH, 0
	MOV CX, 2
	MOV BL, 0AH
	
	BACK:
		MOV AH, 0
		DIV BL
		MOV BYTE PTR BCD[SI], AH
		INC SI
		LOOP BACK
		
	MOV SI, 0
	XCHG AH, AL
	MOV CL, 04
	ROL AL, CL
	ADD AL, BYTE PTR BCD[SI]
	MOV BCD, AX
	
	
	MOV AH, 4CH
	INT 21H
CODE ENDS
END START
