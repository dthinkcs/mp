DATA SEGMENT
	A DB 73H
	B DB 49H
	SUM DB 0
	CARRY DB 00H
DATA ENDS

CODE SEGMENT
ASSUME CS:CODE, DS:DATA
START:
	MOV DX, DATA
	MOV DS, DX
	
        MOV AL, A
        ADD AL, B
	DAA
        MOV SUM, AL
	ADC CARRY, 0
	
	MOV AH, 4CH
	INT 21H

CODE ENDS
END START
