DATA SEGMENT
	A DD 9F62C06BH
	B DD 0A2ED3921H
	SUM DD 0
	CARRY DB 00H
DATA ENDS

CODE SEGMENT
ASSUME CS:CODE, DS:DATA
START:
	MOV DX, DATA
	MOV DS, DX
        MOV CX, 2
	MOV SI, 0
	CLC
	
	BACK: 
		MOV AX, WORD PTR A[SI]
		ADC AX, WORD PTR B[SI]
                MOV SUM[SI], AX
                
		INC SI
		INC SI
		LOOP BACK
		
	ADC CARRY, 0
	
	MOV AH, 4CH
	INT 21H

CODE ENDS
END START
