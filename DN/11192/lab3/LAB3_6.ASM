DATA SEGMENT
        A DT 0A8ED39214B9F62C06B75H
        B DT 9F62C06B75A8ED39214BH
	DIFF DT ?
	BRW DB 00H
DATA ENDS

CODE SEGMENT
ASSUME CS:CODE, DS:DATA
START:
	MOV DX, DATA
	MOV DS, DX
	
	MOV SI, 0
        MOV CX, 5
	CLC
	
	BACK:
                MOV AX, WORD PTR A[SI]
                SBB AX, WORD PTR B[SI]
                MOV WORD PTR DIFF[SI], AX
		INC SI
                INC SI
                LOOP BACK
	
	ADC BRW, 0
	
	MOV AH, 4CH
	INT 21H

CODE ENDS
END START
