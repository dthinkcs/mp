DATA SEGMENT
	SUM DW 0
	CARRY DB 00H
DATA ENDS

CODE SEGMENT
ASSUME CS:CODE, DS:DATA
START:
	MOV DX, DATA
	MOV DS, DX
	
        MOV AX, 0A56BH
        MOV BX, 0F9A1H
	
	ADD SUM, AX
	ADD SUM, BX
	ADC CARRY, 0
	
	MOV AH, 4CH
	INT 21H

CODE ENDS
END START
