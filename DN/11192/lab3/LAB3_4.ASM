DATA SEGMENT
	A DW 0A56BH
	B DW 0F9A1H
	DIFF DW ?
	BRW DB 00H
DATA ENDS

CODE SEGMENT
ASSUME CS:CODE, DS:DATA
START:
	MOV DX, DATA
	MOV DS, DX

        MOV AX, A
        SUB AX, B
        MOV DIFF, AX
	ADC BRW, 0
	
	MOV AH, 4CH
	INT 21H

CODE ENDS
END START
