DATA SEGMENT
        PRODUCT DD ?
        A DW 0A1B5H
	B DW 3C9FH
DATA ENDS

CODE SEGMENT
ASSUME CS:CODE, DS:DATA
START:
	MOV DX, DATA
	MOV DS, DX
	
    	MOV AX, A
	IMUL B
	MOV SI, 0
        MOV WORD PTR PRODUCT[SI], AX
	INC SI
        INC SI
        MOV WORD PTR PRODUCT[SI], DX
	
	MOV AH, 4CH
	INT 21H

CODE ENDS
END START
