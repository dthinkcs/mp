DATA SEGMENT
	PRODUCT DW ?
	A DB 0AH
	B DB 9FH
DATA ENDS

CODE SEGMENT
ASSUME CS:CODE, DS:DATA
START:
	MOV DX, DATA
	MOV DS, DX
	
        MOV AL, A
	IMUL B
	MOV PRODUCT, AX
	
	MOV AH, 4CH
	INT 21H

CODE ENDS
END START
