DATA SEGMENT
	A DW 7923H
	B DW 1096H
	DIFF DW ?
	BRW DB 00H
DATA ENDS

CODE SEGMENT
ASSUME CS:CODE, DS:DATA
START:
	MOV DX, DATA
	MOV DS, DX
	
	MOV SI, 0
	MOV CX, 2
	CLC
	
	BACK:
		MOV AL, BYTE PTR A[SI]
		SBB AL, BYTE PTR B[SI]
		DAS
		MOV BYTE PTR DIFF[SI], AL
		INC SI
		LOOP BACK
	
	ADC BRW, 0
	
	MOV AH, 4CH
	INT 21H

CODE ENDS
END START
