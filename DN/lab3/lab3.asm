DATA SEGMENT
	SUM DW ?
	CARRY DB 00H
DATA ENDS

CODE SEGMENT
ASSUME CS:CODE DS:DATA
START:
	MOV DX, DATA
	MOV DS, DX
	
	MOV AX, A56BH
	MOV BX, F9A1H
	
	LEA SI, SUM
	MOV [SI], AL
	SUB [SI], BL
	DAS
	
	MOV 2[SI], AH
	SBB 2[SI], BH
	DAS
	
	ADC CARRY, 0
	
	MOV AH, 4CH
	INT 21H

CODE ENDS
END START