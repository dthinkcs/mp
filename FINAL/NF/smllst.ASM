DATA SEGMENT
	ARRAY1 DB 12H,32H,45H,43H,65H,10H
	SMALL DB ?
DATA ENDS
CODE SEGMENT
	ASSUME CS:CODE,DS:DATA
	START:
	MOV AX,DATA
	MOV DS,AX
	LEA SI,ARRAY1
	MOV BL,[SI]
	INC SI
	MOV CX,0
	MOV CL,5
	MOV AH,00H
	BACK:DEC CL
	MOV AL,[SI]
	CMP AL,BL
	JC DOWN
	INC SI
	CMP CL,0
	JE EXIT
	JMP BACK

	DOWN:MOV BL,AL
	CMP CL,0
	JE EXIT
	JMP BACK
	EXIT:MOV SMALL,BL
	MOV AH,4CH
	INT 21H
CODE ENDS
END START

